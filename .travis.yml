sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t robertbehm/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true robertbehm/docker-react npm run test
